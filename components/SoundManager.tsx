
import React from 'react';

// This component is intentionally left empty and not rendered as per user request to have "only zen bell".
const SoundManager: React.FC = () => {
  return null;
};

export default SoundManager;
